name: script-runner-ci

on:
  push:
    branches:
      - main
jobs:
  script-runner:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: make script executable
        run: chmod +x Phase-4/bash_github_actions.sh
    
      - name: running bash script
        run: ./Phase-4/bash_github_actions.sh
      
